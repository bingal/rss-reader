{
  "default": {
    "identifier": "default",
    "description": "Default capabilities for the main window",
    "local": true,
    "windows": ["main"],
    "permissions": ["core:default", "opener:allow-open-url"]
  },
  "shell-sidecar": {
    "identifier": "shell-sidecar",
    "description": "Capability for shell sidecar",
    "local": true,
    "windows": ["main"],
    "permissions": ["shell:allow-execute", "shell:allow-kill"]
  }
}
